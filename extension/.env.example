# Copy to .env (gitignored) if you want to build against something other
# than the local Next.js dev server. Rebuild (`npm run build`) after
# changing this — it's baked into host_permissions and the bundled code at
# build time, not read at runtime. See build.mjs.
WEB_APP_ORIGIN=http://localhost:3000
