{
  "name": "next-ep-lock-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 2 MVP: locks reanime.to until enough job postings are marked applied.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5"
  }
}
